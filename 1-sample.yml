- name: Install Nginx
  become: true
  hosts: web
  tasks:  
    - name: Install nginx
      ansible.builtin.yum:
        name: httpd
        state: latest
        
    - name: start nginx
      ansible.builtin.systemd:
        name: nginx
        state: started
        enabled: yes
